"use client";
import { FormEvent, useState } from "react"; import { AppShell } from "@/components/app-shell"; import { Card, SectionTitle } from "@/components/ui"; import { useAuth } from "@/components/auth-provider";
export default function ProfilePage() { const { user } = useAuth(); const [saved, setSaved] = useState(false); const save = (event: FormEvent) => { event.preventDefault(); setSaved(true); }; return <AppShell title="Profile"><SectionTitle title="Your profile" description="Keep your information current so Sahaya can support you better."/><Card className="max-w-3xl"><form onSubmit={save} className="grid gap-5 md:grid-cols-2"><label className="font-semibold">Name<input required defaultValue={user?.name || ""} className="input mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" /></label><label className="font-semibold">Email<input required type="email" defaultValue={user?.email || ""} className="input mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" /></label><label className="font-semibold">Phone<input type="tel" pattern="[0-9+() -]{7,}" className="input mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" placeholder="+91 98765 43210" /></label><label className="font-semibold">Emergency contact<input className="input mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" placeholder="Name and phone number" /></label><label className="font-semibold md:col-span-2">Wheelchair type<select className="input mt-2 w-full rounded-xl border border-slate-300 px-4 py-3" defaultValue=""><option value="" disabled>Select wheelchair type</option><option>Manual wheelchair</option><option>Power wheelchair</option><option>Sports wheelchair</option><option>Other mobility device</option></select></label>{saved && <p role="status" className="md:col-span-2 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">Profile preferences saved for this session.</p>}<button className="w-fit rounded-xl bg-brand-700 px-5 py-3 font-bold text-white">Save profile</button></form></Card></AppShell>; }
