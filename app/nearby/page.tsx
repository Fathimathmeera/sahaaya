"use client";
import { AppShell } from "@/components/app-shell"; import { Card, SectionTitle } from "@/components/ui";
const places = [{ name: "Green Leaf Café", detail: "Step-free entrance · Accessible washroom", score: "92" }, { name: "Central Library", detail: "Ramp · Elevator · Accessible parking", score: "96" }, { name: "Metro Station", detail: "Elevator availability reported", score: "78" }];
export default function NearbyPage() { return <AppShell title="Nearby Places"><SectionTitle title="Accessible nearby places" description="Community-sourced sample recommendations near your current area."/><div className="grid gap-5 md:grid-cols-3">{places.map((place) => <Card key={place.name}><div className="flex items-start justify-between"><h3 className="font-bold">{place.name}</h3><span className="rounded-full bg-emerald-100 px-2 py-1 text-sm font-bold text-emerald-800">{place.score}</span></div><p className="mt-4 text-sm text-slate-600">{place.detail}</p><button className="mt-5 font-bold text-brand-700">View details →</button></Card>)}</div></AppShell>; }
