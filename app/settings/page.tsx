"use client";
import { useRouter } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { Card, SectionTitle } from "@/components/ui"; import { useAuth } from "@/components/auth-provider";
export default function SettingsPage() { const { logout } = useAuth(); const router = useRouter(); const signOut = async () => { await logout(); router.push("/login"); }; return <AppShell title="Settings"><SectionTitle title="Settings" description="Choose how Sahaya works for you."/><div className="max-w-3xl space-y-4"><Card><label className="flex items-center justify-between gap-4"><span><span className="block font-bold">Dark mode</span><span className="text-sm text-slate-600">Use a darker interface.</span></span><input type="checkbox" className="h-5 w-5 accent-brand-700" /></label></Card><Card><label className="block font-bold">Language<select defaultValue="English" className="input mt-3 w-full rounded-xl border border-slate-300 p-3"><option>English</option><option>Hindi</option><option>Tamil</option></select></label></Card><Card><label className="flex items-center justify-between gap-4"><span><span className="block font-bold">Notifications</span><span className="text-sm text-slate-600">Receive route and reminder alerts.</span></span><input defaultChecked type="checkbox" className="h-5 w-5 accent-brand-700" /></label></Card><Card><label className="flex items-center justify-between gap-4"><span><span className="block font-bold">Privacy</span><span className="text-sm text-slate-600">Keep location sharing under your control.</span></span><input defaultChecked type="checkbox" className="h-5 w-5 accent-brand-700" /></label></Card><button onClick={signOut} className="rounded-xl bg-red-600 px-5 py-3 font-bold text-white">Log out</button></div></AppShell>; }
