"use client";
import { AppShell } from "@/components/app-shell";
import { ActionCard, Card } from "@/components/ui";
import { useAuth } from "@/components/auth-provider";
import { FaMapMarkedAlt, FaCamera, FaExclamationTriangle, FaMicrophone, FaUsers, FaSearch, FaParking, FaHeart, FaIdCard } from "react-icons/fa";
const cards=[['AI Navigation','Find barrier-free routes',FaMapMarkedAlt,'/navigation'],['Accessibility Scanner','Check a place before you go',FaCamera,'/scanner'],['AI Voice Assistant','Hands-free everyday support',FaMicrophone,'/health'],['Emergency SOS','Share your location quickly',FaExclamationTriangle,'/emergency'],['Community Reports','Access local insight',FaUsers,'/community'],['Health Assistant','Manage wellbeing reminders',FaHeart,'/health'],['Smart Parking','Locate accessible parking',FaParking,'/nearby'],['Travel Planner','Plan a smoother journey',FaIdCard,'/navigation']];
export default function Dashboard(){const {user}=useAuth();const name=user?.name||"there";return <AppShell title="Dashboard"><section className="rounded-3xl bg-gradient-to-r from-brand-700 to-emerald-600 p-7 text-white"><p className="text-lg">Hello, {name} 👋</p><h2 className="mt-2 text-3xl font-bold">Welcome back to Sahaya</h2><p className="mt-1 text-blue-50">Your AI Accessibility Companion.</p><label className="relative mt-6 block max-w-xl"><FaSearch className="absolute left-4 top-4 text-slate-500"/><input aria-label="Quick search" placeholder="Search accessible places, routes, and services" className="w-full rounded-xl py-3 pl-11 pr-4 text-slate-900"/></label></section><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([title,desc,Icon,href])=><ActionCard key={title as string} title={title as string} description={desc as string} icon={Icon as typeof FaMapMarkedAlt} href={href as string}/>)}</div><div className="mt-8 grid gap-5 lg:grid-cols-3"><Card className="lg:col-span-2"><h3 className="text-lg font-bold">Your accessibility snapshot</h3><div className="mt-5 grid gap-4 sm:grid-cols-3">{[['Route readiness','Excellent','emerald'],['Saved places','12','blue'],['Community updates','4 new','amber']].map(([a,b,c])=><div key={a} className={`rounded-xl bg-${c}-50 p-4`}><p className="text-sm text-slate-600">{a}</p><p className="mt-2 text-2xl font-bold">{b}</p></div>)}</div></Card><Card><h3 className="font-bold">Next reminder</h3><p className="mt-4 text-sm text-slate-600">Hydration reminder</p><p className="text-2xl font-bold text-brand-700">2:00 PM</p></Card></div></AppShell>}
