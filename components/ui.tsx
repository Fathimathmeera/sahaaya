import { ReactNode } from "react";
import { IconType } from "react-icons";
export function Card({ children, className = "" }: { children: ReactNode; className?: string }) { return <div className={`surface rounded-2xl border border-slate-200 bg-white p-5 shadow-card ${className}`}>{children}</div>; }
export function SectionTitle({ title, description }: { title: string; description?: string }) { return <div className="mb-6"><h2 className="text-2xl font-bold">{title}</h2>{description && <p className="muted mt-1 text-slate-600">{description}</p>}</div>; }
export function ActionCard({ title, description, icon: Icon, href, tone = "blue" }: { title: string; description: string; icon: IconType; href: string; tone?: "blue" | "green" | "red" }) { const tones = { blue: "bg-brand-50 text-brand-700", green: "bg-emerald-50 text-emerald-700", red: "bg-red-50 text-red-700" }; return <a href={href} className="surface group rounded-2xl border border-slate-200 bg-white p-5 shadow-card transition hover:-translate-y-1 hover:shadow-lg"><span className={`mb-4 grid h-12 w-12 place-items-center rounded-xl text-xl ${tones[tone]}`}><Icon aria-hidden /></span><h3 className="font-bold">{title}</h3><p className="muted mt-1 text-sm text-slate-600">{description}</p></a>; }
